import { ImageUtils } from './image.utils';

describe('ImageUtils', () => {
  // it('should create image', () => {
  //   let src: string = "some source";
  //   let image = ImageUtils.preloadImage(src);
  //   expect(image.src).toEqual(src);
  // });
})
