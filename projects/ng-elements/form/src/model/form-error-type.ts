export enum FormErrorType {
  PATTERN = 'pattern',
  MIN = 'min',
  MAX = 'max',
  REQUIRED = 'required',
  STEP = 'step',
  MIN_LENGTH = 'minlength',
  MAX_LENGTH = 'maxlength',
  EMAIL = 'email',
  SERVER = 'server'
}
